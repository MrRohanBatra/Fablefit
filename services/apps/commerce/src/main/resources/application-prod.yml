spring:
  datasource:
    url: ${COMMERCE_DB_URL}
    username: ${COMMERCE_DB_USERNAME}
    password: ${COMMERCE_DB_PASSWORD}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${COMMERCE_JWT_ISSUER_URI}

server:
  port: ${COMMERCE_PORT:8082}
